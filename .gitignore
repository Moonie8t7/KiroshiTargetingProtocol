docs/evidence/
tools/.deploy-manifests/
*.disabled

# Release archives built by tools/package.ps1. Derived from source and reproducible
# from any tagged commit, so the tag is the record rather than the binary.
dist/

# Throwaway CET console snippets used while diagnosing a single session.
kstp-cet-*.lua

# WolvenKit build outputs. Pack regenerates them, and the result that ships is committed
# separately as src/archive/pc/mod/kstp.archive (ADR 0015).
src/archive/source/**/packed/
src/archive/source/**/.projectFiles/
src/archive/source/**/*.zip
src/archive/source/**/layout.xml
